# JavaDNSResolverAndNameServer
A Java-based DNS system with a StubResolver for sending queries, a Resolver for iterative resolution with CNAME and timeout handling, and a multithreaded NameServer supporting caching of positive and negative responses, TTL management, and multiple simultaneous clients.
